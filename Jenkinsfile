pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                echo 'Installing mvn and npm tools'
            }
        }
        stage('Depoly') {
            steps {
                echo 'Deploy java and node.js app'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing the new application'
            }
        }        
    }
}
